# Copy to .env.local and fill values.
# Local Docker Postgres (see docker-compose.yml):
DATABASE_URL=postgres://docs:docs@localhost:5432/docs

# iron-session encryption key — MUST be >= 32 chars.
# Generate one with: openssl rand -base64 32
SESSION_PASSWORD=replace-with-a-random-string-at-least-32-chars
